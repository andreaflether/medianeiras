<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <!-- Tell the browser to be responsive to screen width -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Centro Comunitário Nossa Senhora Medianeira | <%= devise_action(action_name, controller_name) %></title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'devise' %>
    <%= javascript_include_tag 'devise' %>
    <%= custom_flash_messages %>

  </head>

  <body class="hold-transition login-page">
    <div class="login-box">
      <div class="login-logo">
        <%= inline_svg_tag("logo.svg", class: "logotipo", style: "cursor: pointer;") %>
      </div>
      <div class="card">
        <div class="card-body login-card-body">
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
