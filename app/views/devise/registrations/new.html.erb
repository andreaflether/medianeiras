<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>Centro Comunitário Nossa Senhora Medianeira | Login</title>
  <!-- Tell the browser to be responsive to screen width -->
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>

<body class="hold-transition login-page">
<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <div class="login-box">
    <div class="login-logo">
      <%= inline_svg("logo.svg", class: "logotipo", style: "cursor: pointer;") %>
    </div>

    <div class="card">
      <div class="card-body login-card-body">
        <p class="login-box-msg">Entre para dar início a sessão</p>

        <form action="../../index3.html" method="post">
          <div class="input-group mb-3">
            <%= f.email_field :email, autofocus: true, autocomplete: "email",
                                      placeholder: "Email", class: "form-control" %>
            <div class="input-group-append">
              <div class="input-group-text">
                <span class="fas fa-envelope"></span>
              </div>
            </div>
          </div>
          <div class="input-group mb-3">
            <%= f.password_field :password, autofocus: true, autocomplete: "password", class: "form-control",
                                            placeholder: "Senha" %>
            <div class="input-group-append">
              <div class="input-group-text">
                <span class="fas fa-lock"></span>
              </div>
            </div>
          </div>
          <div class="row">
            <div class="col-12 text-center">
              <div class="icheck-primary">
                <input type="checkbox" id="remember">
                <label for="remember">
                  Lembre-se de mim
                </label>
              </div>
            </div>

            <div class="col-12">
              <!-- <button type="submit" class="btn btn-primary btn-flat btn-block mt-3">Entrar</button> -->
              <%= f.submit "Entrar", class: "btn btn-primary btn-flat btn-block mt-3" %>
            </div>

          </div>
        </form>

        <p class="mt-3 mb-1 text-center">
          <a href="forgot-password.html">Esqueci minha senha</a>
        </p>
        <p class="mb-0 text-center">
          <%= link_to 'Criar conta', new_user_registration_path, class: "text-center" %>
        </p>
      </div>

    </div>
  </div>
<% end %>
</body>
</html>
