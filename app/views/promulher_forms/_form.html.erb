
<%= simple_form_for(@promulher_form) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :family_size %>
    <%= f.input :f_children %>
    <%= f.input :m_children %>
    <%= f.input :works %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
